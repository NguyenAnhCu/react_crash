import React from 'react'

function Columns() {
    const items = []
    return (
        <> 
            <td>Name</td>
            <td>Anh</td>
        </>
    )
}

// thay vi dung React.Fragment, co the de trong <> va </> nhung trong truong hop do, ta ko the pass in key attribut
export default Columns
